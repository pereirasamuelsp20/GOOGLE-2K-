# ── Production Environment Variables ──
# When deploying behind the same domain (backend serves frontend),
# use a relative /api path so the browser hits the same origin.
VITE_API_BASE=https://google-2k-1.onrender.com
